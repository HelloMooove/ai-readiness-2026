// Copy this file to `config.local.js` and fill in your real values.
// `config.local.js` is gitignored so secrets stay out of the repo.
window.APP_CONFIG = {
  airtableUrl: 'https://api.airtable.com/v0/<your-base-id>/<your-table-id>',
  airtableToken: 'pat...your_token_here...',
};
